# Current Plan

## Контекст
- Предыдущий блок (результат задачи + автозакрытие эпиков) завершён.
- Roadmap: Stage 0 закрыт (RD-001, RD-002, RD-003, RD-105 — DONE). Stage 1 (Installer) — следующие задачи в статусе NEW.

## Что делаем дальше (приоритеты)

1. **Завершить текущий цикл** (если ещё не сделано)
   - Push изменений в `origin/master`.
   - Выполнить `./scripts/vps/verify-sync.sh` и убедиться в синхронизации деплоя и health.

2. **Выбрать следующую задачу roadmap**
   - **RD-101** — Определить контракт сервиса установщика (API, идемпотентность, модель маппингов). Зависит от RD-002 (DONE). Логичная следующая задача Stage 1.
   - **RD-106** — Создать V2 после одобрения V1 (по документации V1 installation milestone уже сделан).
   - Альтернатива: **RD-102** / **RD-103** / **RD-104** после RD-101.

3. **Перед стартом задачи**
   - Активировать минимальный набор скиллов (navigate, develop-b24-python, develop-b24-frontend по необходимости; bitrix24-project-ops при смене статусов).
   - Зафиксировать выбранную задачу в плане и при переходе в работу обновить `ROADMAP_EXECUTION_STATUS.json` и синхронизировать Bitrix24.

## Дисциплина контекста
- Не подгружать лишние документы/скиллы в контекст.
- План и решения вести в `.agent/`; в чате — краткие отсылки и выжимки.
